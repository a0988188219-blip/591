# 新手設定教學(完全不會寫程式也沒關係)

這份教學假設你在**公司電腦(Windows)**上操作，而且會連進 KEIS 內部系統
（因為那是內網，只有公司電腦連得到）。整個安裝只要做一次，之後每次要
上架物件，只需要重複最後那個「執行上架」的指令。

流程是：程式打開瀏覽器 → **你自己登入 KEIS、點到要上架那筆案件的
「詳情」頁**（案件管理 → 圖片下載）→ 回到命令提示字元按 Enter，程式
接手自動抓那筆案件的資料跟照片 → 打開另一個瀏覽器 → **你自己登入
591、點到「刊登賣屋」的表單頁** → 按 Enter，程式接手自動把資料跟
照片填進表單 → 停下來讓你檢查 → 你自己按送出（或之後信任了，讓
程式自動送出）。

登入跟導覽這幾步之所以要你自己動手，是因為 KEIS 和 591 這兩個網站
我都沒辦法事先看到畫面長怎樣、也沒辦法安全地讓程式自動判斷「有沒有
登入成功」，讓你自己操作反而更穩定，程式只負責比較花時間、重複性高
的抓資料/填表單部分。

---

## 第一步：安裝 Python

1. 打開瀏覽器，到 https://www.python.org/downloads/ 下載最新版 Python（點黃色的 "Download Python" 按鈕）
2. 打開下載好的安裝檔
3. **非常重要**：安裝畫面最下面有個勾選框「Add python.exe to PATH」，**一定要打勾**，再按「Install Now」

---

## 第二步：下載這支程式

1. 用瀏覽器打開你的 GitHub 專案頁面：`https://github.com/a0988188219-blip/591`
2. 找一個綠色按鈕「Code」，點下去，選「Download ZIP」
3. 下載完後，把 ZIP 解壓縮到一個你好找的地方，例如「本機磁碟(C:) > 591上架程式」

---

## 第三步：打開「命令提示字元」，安裝程式需要的套件

1. 按開始選單，打字「cmd」，打開「命令提示字元」（一個黑底白字的視窗，不用怕，等等貼指令就好）
2. 輸入以下指令，把路徑換成你解壓縮的位置，按 Enter：
   ```
   cd C:\591上架程式
   ```
3. 接著依序貼上這兩行指令，每貼一行按一次 Enter，等它跑完再貼下一行（第一次會跑比較久，正常）：
   ```
   pip install -r requirements.txt
   ```
   ```
   playwright install chromium
   ```

做完這三步，「安裝」就完成了，之後不用再做這一步。

---

## 第四步：執行上架

在命令提示字元輸入：
```
python main.py post
```

接下來會依序發生：

1. **跳出瀏覽器視窗**，自動打開 KEIS 的「圖片下載」頁面
2. **你自己手動登入**（第一次要登，之後程式會記住，不用每次都登）
3. **你自己點到要上架那筆案件的「詳情」**，把畫面停在那一頁
4. 回到命令提示字元，會看到它在等你：
   ```
   請在瀏覽器視窗中登入 KEIS（如果還沒登入），
   在「案件管理 → 圖片下載」頁面點到你要上架那筆案件的「詳情」，
   準備好之後回到這裡按 Enter 繼續...
   ```
   準備好了就按 **Enter**
5. 程式自動抓那筆案件的資料跟照片，接著會問「還要選下一筆案件嗎？(y/n)」——還有其他物件要上架就打 `y` 按 Enter，重複第 3-4 步選下一筆；沒有的話按 Enter（等於 n）結束選案件
6. **跳出另一個瀏覽器視窗** → **你自己登入 591**，並**手動導覽到「刊登賣屋」的新增/編輯表單頁面**
7. 回到命令提示字元，一樣會等你按 Enter：
   ```
   請在瀏覽器視窗中登入 591（含任何驗證碼/簡訊驗證），
   並手動導覽到「刊登賣屋」的新增/編輯物件表單頁面，
   準備好之後回到這裡按 Enter，程式會開始在目前這頁自動填表...
   ```
8. 按 Enter 後，程式自動把資料跟照片填進表單，**填完後不會自動送出**，停在畫面上讓你自己檢查
9. 檢查沒問題，**你自己在瀏覽器裡手動按送出**

之後如果你已經確認流程都正確、放心讓程式自動送出，改用：
```
python main.py post --publish
```
這樣最後一步會自動幫你按送出，不用自己按。

---

## 之後每次要上架，只需要：

1. 打開命令提示字元，`cd` 到程式資料夾
2. 輸入 `python main.py post`（或已經信任的話用 `python main.py post --publish`）
3. 照上面步驟，自己點到案件詳情、按 Enter，選填要不要繼續下一筆

---

## 如果卡住了、跳出錯誤訊息怎麼辦？

**不用自己排查**，把命令提示字元視窗裡的錯誤文字整段複製，或是螢幕截圖，直接傳給我，我幫你看。這是正常過程的一部分——KEIS 和 591 網站的表單長相我這邊沒辦法事先看到，第一次執行很可能需要跟我來回校正一兩次（例如案件清單抓不到、某個欄位填不進去）。

---

## 備用方式：Excel（不需要連 KEIS，暫時用不到可略過）

如果哪天 KEIS 連不上或想手動輸入單筆資料，也可以改用 Excel：

```
python main.py template
# 打開 data/listings_template.xlsx 填資料
python main.py post --source excel --file data/listings_template.xlsx
```
