# 新手設定教學(完全不會寫程式也沒關係)

這份教學假設你在**公司電腦(Windows)**上操作。整個設定只要做一次，
之後每次要上架物件，只需要重複「填 Excel → 執行指令」這兩步。

---

## 第一步：安裝 Python

1. 打開瀏覽器，到 https://www.python.org/downloads/ 下載最新版 Python（點黃色的 "Download Python" 按鈕）
2. 打開下載好的安裝檔
3. **非常重要**：安裝畫面最下面有個勾選框「Add python.exe to PATH」，**一定要打勾**，再按「Install Now」
4. 裝完後電腦會多一個叫「命令提示字元」的東西，等等會用到

---

## 第二步：下載這支程式

1. 用瀏覽器打開你的 GitHub 專案頁面：`https://github.com/a0988188219-blip/591`
2. 找一個綠色按鈕「Code」，點下去，選「Download ZIP」
3. 下載完後，把 ZIP 解壓縮到一個你好找的地方，例如「本機磁碟(C:) > 591上架程式」

---

## 第三步：打開「命令提示字元」，安裝程式需要的套件

1. 按開始選單，打字「cmd」，打開「命令提示字元」（一個黑底白字的視窗，不用怕，等等貼指令就好）
2. 輸入以下指令，把資料夾路徑換成你剛剛解壓縮的位置，按 Enter：
   ```
   cd C:\591上架程式
   ```
3. 接著依序貼上這三行指令，每貼一行按一次 Enter，等它跑完再貼下一行（第一次會跑比較久，正常）：
   ```
   pip install -r requirements.txt
   ```
   ```
   playwright install chromium
   ```

做完這三步，「安裝」就完成了，之後不用再做這一步。

---

## 第四步：產生 Excel 物件資料範本

在同一個命令提示字元視窗輸入：
```
python main.py template
```

執行完，資料夾裡會多一個檔案：`data\listings_template.xlsx`。用 Excel 打開它。

---

## 第五步：把物件資料填進 Excel

Excel 裡已經有一列範例資料，照著格式改成你自己的物件即可，一列 = 一筆物件：

| 欄位 | 說明 |
|---|---|
| title | 物件標題，例如「高雄前鎮景觀三房」|
| city / district / address | 縣市 / 行政區 / 地址 |
| house_type | 電梯大樓 / 公寓 / 透天厝 / 華廈 / 套房 |
| total_price_wan | 總價（萬元），例如 880 |
| main_building_ping / total_ping / land_ping | 主建物坪數 / 權狀總坪數 / 土地坪數（沒有土地可留空）|
| rooms / living_rooms / bathrooms | 幾房 / 幾廳 / 幾衛 |
| floor / total_floors | 所在樓層 / 總樓層 |
| age_years | 屋齡 |
| parking / facing | 車位說明 / 座向 |
| description | 物件介紹文字 |
| contact_name / contact_phone | 聯絡人姓名 / 電話 |
| photos | 照片檔案在你電腦裡的路徑，多張用分號 `;` 隔開，例如：`C:\照片\客廳.jpg;C:\照片\房間.jpg` |

填完存檔（保持 .xlsx 格式）。

---

## 第六步：執行上架（第一次會需要手動登入一次）

回到命令提示字元，輸入：
```
python main.py post --source excel --file data/listings_template.xlsx
```

接下來會發生：
1. 電腦自動跳出一個瀏覽器視窗
2. **第一次執行**：畫面會停在 591 登入頁，這時候**你自己在那個瀏覽器視窗手動輸入帳號密碼登入**（跟平常登入網站一樣）。登入完成後程式會自動偵測到，接著自動幫你把表單填好、照片上傳好
3. 登入過一次之後，以後執行就不用再登入了（程式記住了）
4. 表單填好後**不會自動送出**，會停在確認頁，讓你自己檢查有沒有問題，看完沒問題**自己手動按送出**

確認流程都順利、資料都正確之後，如果你希望之後讓程式**自動幫你按送出**（不用每次自己按），改用這個指令：
```
python main.py post --source excel --file data/listings_template.xlsx --publish
```

---

## 之後每次要上架新物件，只需要：

1. 打開 Excel 範本，改成新物件的資料，存檔
2. 打開命令提示字元，`cd` 到程式資料夾
3. 貼上第六步的指令，按 Enter

---

## 如果卡住了、跳出錯誤訊息怎麼辦？

**不用自己排查**，把命令提示字元視窗裡的錯誤文字整段複製，或是螢幕截圖，直接傳給我，我幫你看。這是正常過程的一部分——591 網站的表單長相我這邊沒辦法事先看到，第一次執行很可能需要跟我來回校正一兩次。

---

## KEIS 內網系統那條路（進階，目前先用 Excel 這條路就好）

`python main.py post --source keis` 這個指令會直接連你們公司的案件系統抓資料，
但這條路的頁面選擇器還沒校正過，建議先用上面 Excel 的方式跑順，
之後有需要我們再一起校正 KEIS 那條路。
